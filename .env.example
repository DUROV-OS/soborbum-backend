DATABASE_URL=postgresql+psycopg2://soborbum:soborbum@db:5432/soborbum
JWT_SECRET=change-me-in-production
STORAGE_DIR=/app/storage
ADMIN_EMAIL=admin@soborbum.local
ADMIN_PASSWORD=admin123
ADMIN_FULL_NAME=Administrator

# AI assistant (app/ai) - required for /api/ai to work at all
ANTHROPIC_API_KEY=
AI_MODEL=claude-sonnet-5
# Remote MCP connector to the knowledge base (OAuth2 client-credentials) -
# leave empty until you have connection details, the AI section works fine
# without it
MCP_SERVER_URL=
MCP_OAUTH_TOKEN_URL=
MCP_OAUTH_CLIENT_ID=
MCP_OAUTH_CLIENT_SECRET=
